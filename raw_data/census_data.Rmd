---
title: "Diss graphs"
author: "Jae Yeon Kim"
date: "\today"
output:
  html_document: default
  pdf_document:
    latex_engine: pdflatex
---

# Setup
```{r, echo=FALSE}

# remove all objects 
rm(list=ls())

# load libraries
pacman::p_load(
          knitr,
          foreign,
          ggplot2,
          reshape,
          reshape2,
          gridExtra,
          plyr,
          dplyr,
          tidyr,
          readstata13,
          xlsx, 
          visreg,
          ggthemes,
          stringr,
          stargazer,
          ggpubr,
          jtools,
          tibble,
          gdata)

source("/home/jae/functions-/theme_publication.r")

```


# File loads
```{r}

# census file
census <- list.files(pattern = "*.xls") # census 

census_data <- do.call("rbind", lapply(census, function(X) read.xls(X, sheet = 1)))

```
